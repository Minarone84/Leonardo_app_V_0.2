[project]
name = "leonardo"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "aiohttp",
    "numpy",
    "pandas",
    "PySide6",
    "websockets",
]

[project.optional-dependencies]
dev = ["pytest>=8.0.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
